#include<iostream>
#include<cmath>

using namespace std;
/*
int N;

int saveQueen[100];

void Nqueen(int k)
{
	if(k==N)
	{
		for(int i=0;i<N;i++)
		{
			cout<<saveQueen[i]+1<<' ';
		}
		cout<<endl;
		return ;
	}
	int i,j;
	for( i=0;i<N;i++)
	{
		for( j=0;j<k;j++)
		{
			if(saveQueen[j]==i||abs(saveQueen[j]-i)==abs(k-j))
			{
				break;
			}
		}
		if(j==k)
		{
			saveQueen[k]=i;
			Nqueen(k+1);
		}
	}
}

int main()
{
	cin>>N;
	Nqueen(0);
	return 0;
 } 3.N皇后*/


 
 /*
 4.逆波兰表达式 
 */
